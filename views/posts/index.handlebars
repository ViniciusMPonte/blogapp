<div class="card">
    <div class="card-body">
        <h1>{{post.title}}</h1>
        <br>
        <small><strong>Data de publicação: </strong>{{post.date}}</small>
        <hr>
        <p>{{post.content}}</p>
    </div>
</div>